const SortCost = () => {
    return (
        <>
            <fieldset className="sort__cost">
                <legend>Цена:</legend>
                <input />
            </fieldset>
        </>
    );
};

export default SortCost;
